<% if @series.poster_processing? %>
  <div class="processing" id="poster">
    <%= image_tag "ajax-loader-2.gif" %>
  </div>
<% else %>
  <div id="poster">
    <%= image_tag @series.poster.url(:medium), :class => "thumbnail",
                  :class => "poster" %>
  </div>
<% end %>